import React from 'react';
import { View, Text } from 'react-native';

export default function Dialog({ children }){
  return (
    <View>
      <Text>Dialog (RN stub)</Text>
      {children}
    </View>
  )
}
