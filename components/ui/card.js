import React from 'react';
import { View, Text } from 'react-native';

export default function Card({ children }) {
  return (
    <View>
      <Text>Card (RN stub)</Text>
      {children}
    </View>
  );
}
