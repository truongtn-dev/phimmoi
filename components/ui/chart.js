import React from 'react';
import { View, Text } from 'react-native';

export default function Chart(){
  return (
    <View>
      <Text>Chart (RN stub) — replace with react-native-chart-kit</Text>
    </View>
  )
}
