import React from 'react';
import { View, Text } from 'react-native';

export default function AlertDialog(props) {
  return (
    <View>
      <Text>AlertDialog (RN stub)</Text>
      {props.children}
    </View>
  );
}
