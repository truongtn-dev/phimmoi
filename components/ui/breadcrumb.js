import React from 'react';
import { View, Text } from 'react-native';

export default function Breadcrumb({ children }) {
  return (
    <View>
      <Text>Breadcrumb (RN stub)</Text>
      {children}
    </View>
  );
}
