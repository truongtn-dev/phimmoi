import React from 'react';
import { View, Text } from 'react-native';

export default function Carousel({ children }) {
  return (
    <View>
      <Text>Carousel (RN stub)</Text>
      {children}
    </View>
  );
}
