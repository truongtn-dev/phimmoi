import React from 'react';
import { View, Text } from 'react-native';

export default function Collapsible({ children }){
  return (
    <View>
      <Text>Collapsible (RN stub)</Text>
      {children}
    </View>
  )
}
